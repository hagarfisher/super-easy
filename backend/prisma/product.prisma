model Product {
    id        Int      @id @default(autoincrement())
    name      String
    brand     String
    quantity  Int
    price     Int
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
